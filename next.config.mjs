/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: "dist", // Change build output folder from `.next` to `dist`
  };
  
  export default nextConfig;
  